Following 
- extension: contains the topx content model and spring configuration.
- web-extension: contains the share config (debug settings and extend the aspect visibility to the hitcounter aspect.
- templates: contains the search webscript configuration which deliveres the json for use in the dashlet.
